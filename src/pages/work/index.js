import {html} from '../../utils';
import {Navigation} from '../../components/navigation';

export const Work = () => {
  return html`
    <div>
        <${Navigation} />
       Work Page
    </div>
  `;
};
