import {h} from 'preact';
import { WeightedLeftIconGrid} from './index';

export default {
  title: 'Weighted Left Icon Grid'
};

const FIXTURE = {};

export const Main = () => {
  return (<WeightedLeftIconGrid {...FIXTURE} />)
};
