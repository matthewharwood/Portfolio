import {h} from 'preact';

import {Polymorph} from './';

export default {
  title: 'Polymorph'
};

const FIXTURE = {
};

export const Main = () => {
  return (
      <Polymorph {...FIXTURE} />
  )
};