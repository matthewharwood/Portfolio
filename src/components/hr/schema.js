import {Header, UI} from '../_parts/header';

export default {
  name: 'hr',
  title: 'Horizontal Rule',
  type: 'document',
  fields: [
    {
      name: 'title',
      title: 'Title',
      type: 'string'
    },
  ]
}
