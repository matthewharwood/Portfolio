import {h} from 'preact';

import {PostFooter} from './';

export default {
  title: 'Post Footer'
};

const FIXTURE = {
};

export const Main = () => {
  return (
      <PostFooter {...FIXTURE} />
  )
};